import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    borderRadius: 5,
    width: 300,
    textAlign: "center",
  },
});

export default styles;
