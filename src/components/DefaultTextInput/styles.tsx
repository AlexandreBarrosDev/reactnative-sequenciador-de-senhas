import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 300,
  },
  input: {
    height: 40,
    width: "100%",
    borderRadius: 5,
    padding: 10,
    fontSize: 16,
    color: "#222",
    backgroundColor: "#ddd",
  },
});

export default styles;
